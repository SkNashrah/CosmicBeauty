<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CosmicBeauty.Views"
             xmlns:viewmodel="clr-namespace:CosmicBeauty.ViewModels"
             NavigationPage.HasNavigationBar="False"
             x:Class="CosmicBeauty.Views.WishlistPage"
             x:DataType="viewmodel:WishlistViewModel"
             Style="{StaticResource CosmicPage}"
             Title="Wishlist">

    <Grid  Grid.Row="0"
           RowDefinitions="Auto,*">
        <Grid ColumnDefinitions="Auto,*,Auto"
              Padding="2"
              Style="{StaticResource CosmicNavBar}">

            <Button Grid.Column="0"
                    Text="←"
                    TextColor="White"
                    FontSize="20"
                    BackgroundColor="Transparent"
                    Command="{Binding GoBackCommand}"
                    HorizontalOptions="Start"/>

            <Label Grid.Column="1"
                   Text="{Binding Title}"
                   TextColor="White"
                   BackgroundColor="Transparent"
                   FontSize="20"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"/>

            <Button Grid.Column="2"
                    Text="⚙️"
                    BackgroundColor="Transparent"
                    FontSize="20"
                    Command="{Binding SettingsCommand}"
                    HorizontalOptions="End"
                    VerticalOptions="Center"/>

        </Grid>
    
        <RefreshView Grid.Row="1" 
                    IsRefreshing="{Binding IsBusy}"
                    Command="{Binding LoadWishlistCommand}">
            <CollectionView ItemsSource="{Binding WishlistItems}"
                            SelectionMode="None"
                            EmptyView="No items in your wishlist.">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"
                                        ItemSpacing="0"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodel:WishlistViewModel">
                        <local:CardView
                            Image="{Binding Image}"
                            Name="{Binding Name}"
                            SalePrice ="{Binding SalePrice}"
                            Product="{Binding .}"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>